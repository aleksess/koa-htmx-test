<form action="<%= form.action %>" method="<%= form.method %>" <%= form.hxMethod + "=" + `${form.hxAction}`%> hx-target="<%= form.hxTarget %>" hx-swap="<%= form.hxSwap %>" >
    <textarea name="comment[content]"></textarea>
    <input type="submit"/>
</form>